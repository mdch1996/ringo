An IoT project with Django, Raspberry Pi, webcam and thermometer sensor

How to Deploy

    $ sudo apt-get install supervisor
    $ sudo apt-get install rabbitmq-server
    $ sudo apt-get install fswebcam

    $ pip install -r requirements